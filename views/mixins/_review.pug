mixin review(review)
 pre= h.dump(review)
  .review__header
    .review__author
      img.avatar(src=review.author.gravatar)
      p= review.author.name
    .review__stars(title=`Rated ${review.rating} our of 5 stars`)
      = `★`.repeat(review.rating)
      = `☆`.repeat(5 - review.rating)
    time.review__time(datetime=review.created)= h.moment(review.created).fromNow()
  .review__body
    p= review.text
    if user.isAdmin
      button.remove-doc(data-id=review) X


  //- script(type="text/javascript").
  //-   let bt = document.querySelector('.remove-doc')
  //-   bt.addEventListener('click', () => {
  //-     var userId = bt.getAttribute('data-id')

  //-     console.log('yoo', userId)
  //-   })
