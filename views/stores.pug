extends layout

include mixins/_storeCard
include mixins/_pagination



block content


    .inner
        br
        h2 #{title}
        br
        br
        br
        //pre= h.dump(stores)
        .stores
            each store in stores
                if store.approved
                    +storeCard(store)
        +pagination(page,pages,count)